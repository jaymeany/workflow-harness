# board.conf: which board this project uses.
#
# BOARD_ADAPTER names a folder under adapters/. BOARD_ID is the project board,
# set during first start. The project CLAUDE.md § Board records the same id for
# the agents.

BOARD_ADAPTER="trello"
BOARD_ID="{{BOARD_ID}}"
